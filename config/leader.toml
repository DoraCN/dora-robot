# config/leader.toml — 主臂守护进程 + Web 控制台配置

[arm]
id = "arm_1"
type = "so101"

[arm.so101]
baud = 1_000_000
ids = [1, 2, 3, 4, 5, 6]
vid = "0x1A86"
pid = "0x55D3"
serial = "5AB0183620"

[zenoh]
mode = "peer"

[console]
bind = "0.0.0.0"
port = 8080
